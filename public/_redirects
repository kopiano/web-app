# Cloudflare Pages automatically serves index.html for SPA routes when no
# top-level 404.html exists. Explicit rewrites to /index.html are avoided
# because Pages canonicalizes that target to / and changes the browser URL.
